name: Docker Image CI

on:
  push:
    branches: [ "poc" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build -t src/frontend/ . --file Dockerfile --tag rontend:$(date +%s)
